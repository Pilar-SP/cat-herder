<h1>Project <%= @project.name %></h1>

<h2>Description</h2>
<%= @project.description %>

<h2>Priority</h2>
<%= @project.priority %>

<div>
  <%= link_to "Delete",
    project_path(@project),
    data: { turbo_method: :delete, turbo_confirm: "Are you sure?"}
  %>
</div>
